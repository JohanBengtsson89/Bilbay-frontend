import { useState } from "react";
import axios from "axios";

const url =  "http://localhost:8081/api/available-products/true"

const [item, setItem] = useState ([])

const Product = () => {
    return (
        null
    )
}

export default Product